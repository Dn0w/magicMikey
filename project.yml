name: magicMikey
options:
  bundleIdPrefix: com.Dn0w
  deploymentTarget:
    iOS: "17.0"
  xcodeVersion: "15.2"

settings:
  base:
    SWIFT_VERSION: "5.9"
    TARGETED_DEVICE_FAMILY: "2"
    IPHONEOS_DEPLOYMENT_TARGET: "17.0"
    SUPPORTS_MAC_DESIGNED_FOR_IPAD: NO

schemes:
  magicMikey:
    build:
      targets:
        magicMikey: all
        magicMikeyKeyboard: all
    run:
      config: Debug
    archive:
      config: Release

targets:
  magicMikey:
    type: application
    platform: iOS
    sources:
      - path: magicMice
    dependencies:
      - target: magicMikeyKeyboard
        embed: true
    info:
      path: magicMice/Resources/Info.plist
      properties:
        CFBundleDisplayName: magicMikey
        CFBundleShortVersionString: "1.0"
        CFBundleVersion: "1"
        LSRequiresIPhoneOS: true
        UIRequiredDeviceCapabilities:
          - ipad
        UISupportedInterfaceOrientations:
          - UIInterfaceOrientationLandscapeLeft
          - UIInterfaceOrientationLandscapeRight
        UISupportedInterfaceOrientations~ipad:
          - UIInterfaceOrientationLandscapeLeft
          - UIInterfaceOrientationLandscapeRight
        UIApplicationSupportsIndirectInputEvents: true
        UILaunchScreen: {}
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.Dn0w.magicMikey
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        CODE_SIGN_IDENTITY: "Apple Development"
        CODE_SIGN_STYLE: Automatic
        DEVELOPMENT_TEAM: MU335F3K6A

  magicMikeyKeyboard:
    type: app-extension
    platform: iOS
    sources:
      - path: magicMice/UI/Keyboard
      - path: magicMice/UI/MacroBar
      - path: magicMice/Models
      - path: magicMice/UI/Settings
      - path: magicMice/Core/InputEngine/ModifierState.swift
      - path: magicMice/Core/InputEngine/HapticEngine.swift
      - path: magicMice/Core/InputEngine/InputRouter.swift
      - path: magicMice/Core/IME
      - path: magicMice/Core/MacroStore.swift
      - path: magicMice/Resources/Assets.xcassets
      - path: magicMice/Resources/pinyin.db
      - path: KeyboardExtension
    info:
      path: KeyboardExtension/Info.plist
      properties:
        CFBundleDisplayName: magicMikey Keyboard
        CFBundleShortVersionString: "1.0"
        CFBundleVersion: "1"
        NSExtension:
          NSExtensionAttributes:
            IsASCIICapable: false
            PrefersRightToLeft: false
            PrimaryLanguage: en-US
            RequestsOpenAccess: true
          NSExtensionPointIdentifier: com.apple.keyboard-service
          NSExtensionPrincipalClass: $(PRODUCT_MODULE_NAME).KeyboardViewController
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.Dn0w.magicMikey.keyboard
        CODE_SIGN_IDENTITY: "Apple Development"
        CODE_SIGN_STYLE: Automatic
        DEVELOPMENT_TEAM: MU335F3K6A
